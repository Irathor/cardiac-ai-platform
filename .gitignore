__pycache__/
*.pyc
.venv/
.venv*/
.pytest_cache/
.ruff_cache/

node_modules/
dist/
*.tsbuildinfo
coverage/
playwright-report/
test-results/

.env
*.egg-info/

.DS_Store
Thumbs.db

# Local Claude Code settings (per-machine permissions), never shared.
.claude/

data/acdc-raw/
data/models/

# Terraform (EPIC-9): local state and real credentials, never versioned.
# terraform.tfvars.example files ARE versioned (no real values) — the glob
# below only matches the un-suffixed real file.
infrastructure/terraform/**/.terraform/
infrastructure/terraform/**/terraform.tfstate
infrastructure/terraform/**/terraform.tfstate.backup
infrastructure/terraform/**/*.tfvars
!infrastructure/terraform/**/*.tfvars.example
infrastructure/terraform/**/crash.log
